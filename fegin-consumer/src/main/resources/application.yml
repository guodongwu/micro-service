server:
  port: 9006

spring:
  application:
    name: fegin-consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
    instance:
      prefer-ip-address: true

feign:
  hystrix:
    enabled: true